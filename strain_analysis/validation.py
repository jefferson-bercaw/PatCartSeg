import numpy as np
from evaluate import load_model

if __name__ == "__main__":
    # Get filenames we're testing and figure out how many pre-post pairs we have
    # Construct list of dictionaries of filenames of filename pairs, subjectID, Fr, duration in each
    # Load in MRI volume

    # Define and load in model using load_model